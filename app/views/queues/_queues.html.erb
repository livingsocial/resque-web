<h1>Queues</h1>
<p class="intro">The list below contains all the registered queues with the number of jobs currently in the queue. Select a queue from above to view all jobs currently pending on the queue.</p>
<table class="queues">
  <tr>
    <th>Name</th>
    <th>Jobs</th>
  </tr>
  <% queue_names.each do |queue_name| %>
  <tr>
    <td class="queue"><%= link_to queue_name, queue_path(queue_name) %></td>
    <td class="size"><%= queue_size(queue_name) %></td>
  </tr>
  <% end %>

  <% failed_queue_names.each_with_index do |queue_name, i| %>
  <tr class="<%= failed_queue_class(queue_name) %><%= " first_failure" if i.zero? %>">
    <td class="queue failed"><%= link_to queue_name, failure_path(queue_name), :class => 'queue' %></td>
    <td class="size"><%= failed_queue_size(queue_name) %></td>
  </tr>
  <% end %>
</table>